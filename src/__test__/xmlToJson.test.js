import xmlToJson from "../UtilityFunctions/xmlToJson.js";
const mockTransform = jest.mock('../UtilityFunctions/xmlToJson.js')
describe("Splitting the XML file reaceived", () => {
    test("Axios loads an XML file from the Resources folder", () => {
        
    })
})